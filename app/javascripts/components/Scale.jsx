var React = require('react');

var Scale = React.createClass({
  render: function() {
    return (
      <h2 className="scale">
        Pentatonic Major
      </h2>
    );
  }
});

module.exports = Scale;